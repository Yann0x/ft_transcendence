{
  "name": "server_proxy",
  "version": "1.0.0",
  "description": "Unique entrypoint for frontend",
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "tsx watch src/game.ts",
    "start": "npm run builld && node build/game.js",
    "clean": "rm -rf build",
    "fclean": "npm run clean && rm -rf node_modules"
  },
  "dependencies": {
    "@fastify/websocket": "^11.0.2",
    "@sinclair/typebox": "^0.34.41",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
