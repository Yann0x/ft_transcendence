{
  "name": "server_proxy",
  "version": "1.0.0",
  "description": "Unique entrypoint for frontend",
  "license": "ISC",
  "author": "",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "tsx watch src/proxy.ts",
    "start": "npm run builld && node build/proxy.js",
    "clean": "rm -rf build",
    "fclean": "npm run clean && rm -rf node_modules"
  },
  "dependencies": {
    "@fastify/http-proxy": "^11.3.0",
    "@sinclair/typebox": "^0.34.41",
    "fastify": "^5.6.2"
  },
  "devDependencies": {
    "@fastify/static": "^8.3.0",
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
