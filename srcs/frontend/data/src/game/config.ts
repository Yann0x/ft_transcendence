// CONFIG - Constantes reseau

// Game (pour affichage du gagnant)
export const WIN_SCORE = 11;

// Server (pour le scaling des coordonnees)
export const SERVER_WIDTH = 800;
export const SERVER_HEIGHT = 600;

// Network
export const RECONNECT_DELAY = 2000;
